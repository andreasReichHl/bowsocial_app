import 'package:flutter/material.dart';

class AppColors {
  // Core
  static const primary = Color(0xFF4B5842);
  static const secondary = Color(0xFFB7CE63);

  // States
  static const success = Color(0xFF8FB339);

  // Surfaces
  static const surface = Color(0xFFC7D59F);
  static const surface1 = Color(0xFFDADDD8); // Light Mode

  // Backgrounds
  static const background = Color(0xFFF0F1EE); // Light Mode
  static const darkBackground = Color(0xFF1F261E); // Dark Mode (abgeleitet aus Primary)

}